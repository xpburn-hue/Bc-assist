# Golden Trajectory Fixtures

Golden fixtures store deterministic trajectory expectations for regression testing.

Each fixture should include:

- solver inputs
- drag model
- integration settings
- sampled trajectory points
- comparison tolerances

Fixtures should be updated only when a solver behavior change is intentional and reviewed.
